# API Configuration
API_PORT=3002
API_HOST=localhost

# Frontend Configuration
VITE_API_URL=http://localhost:3002/api

# Database (SQLite path for connection storage)
DATABASE_PATH=./data/connections.sqlite

# Encryption key for storing connection strings (generate a secure key)
ENCRYPTION_KEY=your-32-character-encryption-key
